name: Deploy Flutter Desktop to Web

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH Connect and Build
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_IP }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # --- CONFIGURATION ---
            REPO_URL="https://github.com/saravanan2k03/attandance_desktop.git"
            # ✅ FIXED: Using /opt/ instead of /root/ for permissions
            TARGET_DIR="/opt/attandance_desktop"
            FLUTTER_BIN="/opt/flutter/bin/flutter"
            
            # --- STEP 1: FIX BROKEN FOLDER ---
            if [ -d "$TARGET_DIR" ] && [ ! -d "$TARGET_DIR/.git" ]; then
              echo "⚠️ Folder corrupted. Deleting..."
              rm -rf "$TARGET_DIR"
            fi

            # --- STEP 2: CLONE OR PULL ---
            if [ ! -d "$TARGET_DIR" ]; then
              echo "🚀 Cloning to /opt/..."
              git clone $REPO_URL $TARGET_DIR
            else
              echo "🔄 Pulling latest changes..."
              cd $TARGET_DIR
              git pull origin main
            fi

            cd $TARGET_DIR

            # --- STEP 3: CREATE 'dotenv' FILE ---
            echo "BasKey=${{ secrets.BAS_KEY }}" > dotenv
            echo "IvKey=${{ secrets.IV_KEY }}" >> dotenv
            echo "prodbaseUrl=${{ secrets.PROD_BASE_URL }}" >> dotenv
            echo "✅ Secrets file 'dotenv' created."

            # --- STEP 4: BUILD SEQUENCE ---
            echo "🧹 Running clean..."
            $FLUTTER_BIN clean
            
            echo "📦 Running pub get..."
            $FLUTTER_BIN pub get

            echo "🔨 Building Web App..."
            $FLUTTER_BIN build web --release --base-href "/hourlydotapp/"

            echo "✅ Deployment Complete."